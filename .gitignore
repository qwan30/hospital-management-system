.DS_Store
.idea/
.vscode/
*.iml

# Node
node_modules/
.next/
dist/
coverage/
playwright-report/
test-results/
*.tsbuildinfo

# Java / Maven
target/
.mvn/wrapper/maven-wrapper.jar

# Environment
# Ignore every .env variant, then re-allow the committed templates. Listing only
# `.env` / `.env.local` / `.env.*.local` left filled-in copies such as
# `.env.demo` or `.env.production` tracked by default.
.env
.env.*
!.env.example
!.env.demo.example

# OS / logs
Thumbs.db
*.log
backend/start/test_output*.txt
backend/start/startup-log.txt

# Python
__pycache__/
*.pyc

# Generated uploads
uploads/
.codex-run/
.gitnexus

# Crash Dumps & Stack Traces
*.stackdump

# Extended Python Caches & Environments
.ruff_cache/
.pytest_cache/
.mypy_cache/
.venv/
venv/
*.pyo
*.pyd

# AI Agent / Code Intelligence Caches
.codegraph/
.gitnexus/

# AI development tooling and per-developer session state. Kept out of version
# control so the tracked tree is the product: these directories were 54% of all
# tracked files (1,124 of 2,206) and dwarfed backend/ and frontend/ combined.
# They still work locally — they are ignored, not deleted.
.agents/
.claude/
.khuym/
.agent/
.codex/
.codex-run/
.superpowers/
.instructions.md
skills-lock.json

# Scratch artifacts that were previously committed by accident.
recent_changes.diff
task.md
history/
test-results/
playwright-report/

